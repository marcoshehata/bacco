# Bacco v2.0 - Apple Detection System
# Requirements for Jetson Thor / JetPack 7.1
# ==========================================
#
# INSTALLAZIONE GPU (RACCOMANDATO):
#   bash setup_gpu.sh        # Installa PyTorch GPU + dipendenze
#   python3.10 main.py video.mp4
#
# PyTorch viene installato separatamente via wheel NVIDIA
# (non incluso qui per evitare conflitti con la versione ottimizzata Jetson)
#
# Performance:
#   - GPU (dopo setup_gpu.sh): ~25-35 FPS
#   - CPU (torch generico):    ~4 FPS
# ==========================================

# Computer Vision
opencv-python==4.10.0.84

# Object Detection
ultralytics==8.3.36  # YOLOWorld + utilities

# Tracking & Visualization
supervision==0.24.0

# Core Dependencies
numpy==1.26.4
Pillow==10.4.0
PyYAML==6.0.2
scipy==1.13.1
tqdm==4.66.5