# Bacco v2.0 - Apple Detection System
# Requirements for Jetson Thor / JetPack 7.1

# ==============================================================================
# CONFIGURAZIONE CPU-ONLY (FALLBACK)
# ==============================================================================
# JetPack 7.1 è troppo nuovo - non esistono wheel PyTorch con CUDA.
# Questa configurazione usa PyTorch CPU-only come fallback temporaneo.
# 
# Performance attese:
# - Con GPU (dopo compilazione PyTorch): ~25-30 FPS
# - Con CPU (questa config): ~4 FPS
# 
# INSTALLAZIONE:
#   python3.10 -m pip install --user -r requirements.txt
# 
# RUN:
#   python3.10 main.py test_video.mp4
# 
# Per GPU: Vedi README.md sezione "Opzione A: PyTorch da Sorgente"
# ==============================================================================

# PyTorch CPU-only (temporaneo fino a wheel NVIDIA per JetPack 7.1)
torch==2.1.0
torchvision==0.16.0

# Computer Vision
opencv-python==4.10.0.84
opencv-contrib-python==4.10.0.84

# Object Detection
ultralytics==8.3.36  # YOLOWorld + utilities

# Tracking & Visualization
supervision==0.24.0

# Core Dependencies
numpy==1.26.4
Pillow==10.4.0
PyYAML==6.0.2
scipy==1.13.1
tqdm==4.66.5